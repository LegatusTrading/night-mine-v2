# Sensitive configuration files
config/*.conf
!config/*.conf.example

# Data directories (wallets, challenges, balances, backups)
data/

# Python virtual environment
.venv/
venv/
__pycache__/
*.pyc
*.pyo

# Mining runtime files
wallets.json
wallets-*.json
balances.json
challenges.json
*.log
.miner.pid
developer_addresses.json
solutions.csv

# HD Wallet files (CRITICAL - Contains private keys!)
hd-wallets/
hd-wallets.backup-*/
wallet-output/
*.prv
*.skey
mnemonic*.txt

# SSH keys and credentials
*.key
*.pem
id_*
!*.pub

# OS files
.DS_Store
Thumbs.db

# Cardano tools (large binaries - auto-downloaded during setup)
lib/cardano-address
lib/cardano-cli
tools/cardano-address*
tools/cardano-cli*
tools/*.tar.gz
/cardano-address
/cardano-cli

# Ashmaize binaries are COMMITTED to repo (small, ~400KB each)
# lib/macos-arm64/ashmaize_py.so
# lib/linux-x64/ashmaize_py.so
# ashmaize_loader.py

# Editor files
.vscode/
.idea/
*.swp
*.swo
*~

# AI workspace (keep documentation, exclude temporary files)
.ai/tmp/
.ai/*/scratch/
